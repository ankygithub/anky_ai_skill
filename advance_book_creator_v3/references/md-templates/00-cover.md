***

type: cover
title: 书籍标题（改为真实书名）
subtitle: 副标题描述（改为真实副标题）
author: 作者名（改为真实作者）
version: 1.0.0
--------------

# 书籍标题（改为真实书名）

> 副标题描述（一句话，subtitle 缺失时作为封面副标题兜底）

> ⚠️ 本文件是封面模板：封面渲染只读取上方 frontmatter 的
> type / title / subtitle / author / version 五个字段，
> 正文内容不参与渲染。初始化后请立即把占位文字改为真实信息。
>
> ⚠️ frontmatter 分隔符必须是行首三个减号 `---`（开头结尾各一行）。
> 不要写成 `***` 或一串长减号——解析器只认标准分隔符，
> 写错会导致 frontmatter 整体失效、封面被当成普通章节渲染（实际事故）。

