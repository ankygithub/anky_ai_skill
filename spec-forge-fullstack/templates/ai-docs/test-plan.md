# 模板：test-plan.md（AI 自测计划）

> 使用说明：AI 执行文档，存放于 `docs/ai-docs/`。**不重复验收断言原文**，用例通过 TC 编号引用同目录 acceptance-criteria.md；覆盖映射表必须 100% 覆盖全部 AC。自包含原则同 spec.md。自测执行后回填"执行结果"列，作为**偏差分析与验收回填的输入**（仅偏差才进 4-变更记录；正常通过不记 CHG）。状态流转：草稿 → 已冻结（确认点 2）。

---

# 自测计划：[项目名称]

| 项 | 内容 |
|---|---|
| 版本 | v1.0 |
| 日期 | YYYY-MM-DD |
| 状态 | 已冻结 |
| 血缘（仅元信息） | 生成自 acceptance-criteria.md vX |
| 与代码同步截止 | |

## 1. 环境与命令

| 项 | 内容 |
|---|---|
| 依赖安装命令 | 例：`npm install` |
| 被测系统启动命令 | 例：`npm run dev`（端口） |
| 测试执行命令 | 例：`npm test` |
| 测试数据准备 | （**必须写死**：几条、什么值、边界数据；执行 AI 不得自造。例：用户表造 3 条，含 1 个禁用、1 个空手机号、1 个超长昵称） |
| 测试工具 | 例：jest / k6 / curl 脚本 |

## 2. 自测用例表

| 用例编号 | 对应 AC | 测试方法（手动/单测/接口/端到端） | 步骤要点 | 预期结果（通过准则） | 优先级 | 执行结果 |
|---|---|---|---|---|---|---|
| TC-1 | AC-1.1 | | | | P0 | 未执行/通过/失败 |

## 3. 覆盖映射表（必须 100%）

> 必须覆盖全部 AC 类型（AC / AC-E / AC-NF）。AC-E 无对应 REQ，REQ 列填"—（EX-x）"；AC-NF 的 REQ 列填对应 REQ-NF-x。

| REQ | AC | TC | 覆盖 |
|---|---|---|---|
| REQ-F-1.1 | AC-1.1 | TC-1, TC-2 | ✅ |
| —（EX-1） | AC-E-1 | TC-8 | ✅ |
| REQ-NF-1 | AC-NF-1 | TC-9 | ✅ |

## 4. 回归范围

（每次代码变更后必须重跑的 TC 集合：全部 P0 + 受影响模块的 P1）

## 5. 缺陷记录

**缺陷分级（全套统一，acceptance-criteria §4 引用）**：一级 = 阻断验收 / 数据丢失 / 安全问题；二级 = 主要功能异常；三级 = 次要问题 / 体验瑕疵。

| 用例 | 现象 | 严重级 | 关联任务 |
|---|---|---|---|
| | | | |
