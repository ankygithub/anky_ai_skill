# 模板：acceptance-criteria.md（AI 验收标准）

> 使用说明：AI 执行文档，存放于 `docs/ai-docs/`。**验收断言的唯一权威位置**（两组文档里只写这一遍）：test-plan.md 从这里机械派生，spec.md 只放索引，给人看的文档里只放摘要。每条 AC 是 Given-When-Then 结构 + 明确 pass/fail 判定准则；禁止"良好/快速/尽量"等不可测量的词。**AC-x.y 编号语义：x=对应 REQ 组号，y=该组序号**（AC-1.1、AC-1.2 都对应 REQ-F-1.x）。自包含原则同 spec.md。状态流转：草稿 → 已冻结（确认点 2）。

---

# 验收标准：[项目名称]

| 项 | 内容 |
|---|---|
| 版本 | v1.0 |
| 日期 | YYYY-MM-DD |
| 状态 | 已冻结 |
| 血缘（仅元信息） | 生成自 1-需求分析.md vX / spec.md vX |
| 与代码同步截止 | |

## 1. 功能验收标准

> 每条需求至少覆盖：1 个正常流 + 1 个异常/边界场景。判定准则含边界值。P0 需求的需求分析 GWT 三行在此展开为完整断言。

### AC-1.1 [标题]（关联 REQ-F-1.1）

- **Given**（前置条件）：
- **When**（触发动作）：
- **Then**（预期结果）：
- **判定准则**：（明确的 pass/fail 判据）

### AC-1.2 [标题]（关联 REQ-F-1.1）

- **Given**：
- **When**：
- **Then**：
- **判定准则**：

## 2. 排除项验收（系统"不应"的行为）

> 与 spec.md §5.2 逐条对应。

### AC-E-1 [标题]（对应 EX-1）

- **Given**：
- **When**：
- **Then**：（系统不得出现 X / 不得允许 Y）
- **判定准则**：

## 3. 非功能验收

> 类目与 1-需求分析 §6 / spec.md §4 一致：性能 / 安全 / 兼容性 / 易用性。

| 编号 | 指标 | 测量方法 | 通过阈值 |
|---|---|---|---|
| AC-NF-1 | 例：P95 响应时间 | 压测脚本（100 并发 5 分钟） | < 500ms |

## 4. 全局通过准则

- 全部 P0 级 AC 通过；P1 级通过率 ≥ 95%（**默认值，确认点 2 时按项目调整**，小项目可放宽到 100% 或按条数定）
- 无一级缺陷遗留（缺陷分级见 test-plan.md §5）
- 排除项验收（AC-E）全部通过
